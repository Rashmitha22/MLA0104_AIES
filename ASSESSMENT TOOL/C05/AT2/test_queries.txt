MACHINE FAULT EXPERT SYSTEM - TEST QUERIES

1) IMBALANCE
?- scenario(1), forward_chain(m1, Faults).
Expected: Faults = [imbalance].

2) SHAFT MISALIGNMENT
?- scenario(2), verify_fault(m2, misalignment).
Expected: true.

3) BEARING WEAR
?- scenario(3), recommendation(m3, Fault, Action).
Expected: Fault = bearing_wear.

4) PRESSURE SYSTEM FAULT
?- scenario(4), forward_chain(m4, Faults).
Expected: Faults = [pressure_system_fault].

5) MOTOR OVERLOAD
?- scenario(5), forward_chain(m5, Faults).
Expected: Faults = [motor_overload].

6) MULTIPLE FAULTS / BACKTRACKING
?- scenario(6), recommendation(m6, Fault, Action).
Expected first solution: Fault = bearing_wear.
Press ; for the next solution.
Expected second solution: Fault = lubrication_failure.

EXPLANATION QUERY
?- scenario(3), explain(m3, Fault, Required, Action).

FORWARD CHAINING QUERY
?- scenario(6), forward_chain(m6, Faults).

BACKWARD CHAINING QUERY
?- scenario(2), verify_fault(m2, misalignment).
